export const selectIsLoading = (state) => state.campers.loading;
export const selectError = (state) => state.campers.error;
export const selectCampersItem = (state) => state.campers.items;
export const selectCamperById = (state) => state.campers.item;
